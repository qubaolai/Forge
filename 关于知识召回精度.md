# **目标定位**

你的核心目标不是做一个“知识管理系统”，而是：

为企业问答、知识搜索、代码助手等 Agent 服务，提供高精度、低噪声、可按需加载的本地知识能力。

而且重点不是：

```text
“如何把所有知识塞进向量库”
```

而是：

```text
“用户提问后，如何精确定位真正需要的知识，再按需提供给 LLM”
```

这是比传统 RAG 更高一级的问题。

------

# **传统 RAG 的局限**

经典 RAG：

```text
文档
  ↓
chunk
  ↓
embedding
  ↓
top-k retrieval
  ↓
LLM
```

存在几个核心问题：

```text
1. 强依赖 chunk 质量
2. chunk 容易语义断裂
3. top-k 缺少文档结构上下文
4. 企业术语/代码/配置精确匹配差
5. 多跳问题容易召回错误
6. 文档版本、状态、可信度无法表达
7. 大文档难处理
```

所以你提出：

能否不完全依赖 RAG，而是让 Agent 按需搜索和定位文档？

这个方向是对的。

------

# **最终推荐方案**

不是：

```text
放弃 RAG
```

也不是：

```text
纯向量数据库
```

而是：

Agentic Retrieval / Document Navigation

即：

```text
RAG 负责候选召回
Agent 负责文档导航和精确定位
```

------

# **最终架构（推荐）**

```text
用户问题
  ↓
Query Understanding
  ↓
Retrieval Router
  ↓
Document Retrieval
  ↓
Document Navigation
  ↓
Local Precise Search
  ↓
Evidence Selection
  ↓
LLM Answer
```

------

# **一、Query Understanding（问题理解）**

先分析：

```text
问题类型
实体
关键词
时间范围
代码符号
系统名
业务模块
```

并做：

```text
query rewrite
query expansion
术语映射
中英文映射
```

例如：

```text
库存回滚
→ rollback
→ compensation
→ release stock
```

------

# **二、Retrieval Router（检索路由）**

根据问题类型选择不同检索方式。

## **适合全文检索（FTS）**

```text
错误码
函数名
类名
接口
配置项
ID
日期
金额
精确术语
日志
```

## **适合向量检索**

```text
概念解释
流程
设计讨论
相似问题
历史方案
会议内容
```

## **适合代码搜索**

```text
symbol
call chain
grep
AST
```

## **适合结构化查询**

```text
表格
报表
数据库导出
```

------

# **三、分层召回（核心）**

不要直接：

```text
全库 chunk retrieval
```

而是：

```text
1. 文档级召回
2. 章节级定位
3. 片段级读取
```

------

## **1. 文档级召回**

召回：

```text
标题
摘要
标签
outline
所属项目
文档类型
```

作用：

找“哪些文档可能相关”。

------

## **2. 章节级定位**

召回：

```text
section
slide
topic
代码模块
```

作用：

找“文档里的哪部分相关”。

------

## **3. 片段级读取**

读取：

```text
paragraph
code block
log
table rows
OCR block
speaker segment
```

作用：

获取最终证据。

------

# **四、为什么仍然需要 chunk**

必须 chunk，因为：

```text
LLM 上下文有限
需要局部读取
需要精确引用
需要 rerank
需要证据定位
```

但 chunk 不应该：

```text
固定 token 一刀切
```

应该：

Structure-aware chunking（结构感知分块）

------

# **推荐 chunk 结构**

```text
Document
  ↓
Section
  ↓
Block
  ↓
Span
```

------

## **不同层用途**

### **Section**

```text
语义导航
向量召回
摘要
```

### **Block**

```text
段落
代码块
表格
speaker segment
```

### **Span**

```text
最小引用单位
```

例如：

```text
page 8 paragraph 3
00:12:31-00:13:05
```

------

# **五、Knowledge Asset 的作用**

你后来澄清后，Knowledge Asset 不再是核心业务对象。

它真正的作用是：

给 retrieval 提供稳定的文档结构和元数据。

不是为了：

```text
“管理知识”
```

而是为了：

```text
“提高召回质量和导航能力”
```

------

# **推荐的 Asset 内容**

```json
{
  "doc_id": "...",
  "title": "...",
  "summary": "...",
  "outline": [],
  "sections": [],
  "metadata": {},
  "version": "...",
  "status": "...",
  "chunks": []
}
```

重点是：

```text
summary
outline
sections
metadata
```

用于导航。

------

# **六、多模态文档处理**

统一成：

```text
Knowledge Asset + Multi Views
```

------

## **文本**

```text
正文
章节
摘要
```

------

## **PDF**

```text
页码
标题层级
OCR
表格
```

------

## **图片**

生成：

```text
caption
OCR
objects
```

------

## **音频/视频**

生成：

```text
transcript
topic segments
timestamps
summary
```

------

## **Excel / 表格**

不要转纯文本。

保留：

```text
schema
sheet
queryable rows
```

------

# **七、存储方案（推荐）**

采用混合架构。

------

## **文件系统**

存：

```text
原文件
OCR
transcript
preview
large parsed content
```

------

## **数据库（SQLite/Postgres）**

存：

```text
metadata
section index
relations
version
review status
paths
references
```

------

## **全文索引（FTS）**

存：

```text
标题
正文
OCR
转写
函数名
错误码
配置项
日志
```

用于：

精确搜索。

------

## **向量索引**

存：

```text
summary
semantic section
topic chunk
FAQ
caption
meeting topic
```

用于：

语义搜索。

------

# **八、为什么不是“纯 RAG”**

因为企业知识最重要的是：

```text
navigation
inspection
local search
precise reading
```

不是：

```text
一次 top-k chunk retrieval
```

------

# **九、最终推荐的核心理念**

你最终应该做的是：

“文档导航型 Agent”

而不是：

“向量数据库问答机器人”

核心流程：

```text
搜索文档
→ 看文档结构
→ 文档内定位
→ 局部读取
→ 证据压缩
→ 回答
```

------

# **十、最终一句话总结**

最终推荐方案：

用结构化文档导航替代纯 chunk RAG，用多阶段检索替代一次性 top-k，用 Agent 工具按需精读局部内容，而不是把整个知识库 embedding 化。