# ──────────────────────────────────────────────────────────────────────────────
# .env.dev.example — 开发环境专属覆盖模板 (拷贝为 .env.dev)
# 启动: APP_ENV=dev make dev
# 加载优先级: Shell > .env.dev > .env > yaml 默认值
# 只放与 .env 不同的字段
# ──────────────────────────────────────────────────────────────────────────────

# ── 调试输出 ──────────────────────────────────────────────────────────────────
APP_LOG_LEVEL=DEBUG
DB_ECHO=true

# ── 限流放宽 ──────────────────────────────────────────────────────────────────
RATE_LIMIT_ENABLED=false

# ── 摘要触发频率调低，加快验证 ────────────────────────────────────────────────
MEMORY_TRIGGER_EVERY_N=3

# ── AdaptiveRun 调试：关并行 ──────────────────────────────────────────────────
# FORGE_ALLOW_PARALLEL=false
