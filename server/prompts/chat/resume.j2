{# Resume turn 提示词.

 由 TurnResumer.prepare 渲染, 作为 current_user_message 喂给 ContextBuilder,
 会被自动包 <current_question>...</current_question> 标签.

 变量:
   suffix_anchor    str   - 你已输出内容的末尾 N 字符 (做"上次写到哪儿"的锚点)
   anchor_chars     int   - suffix_anchor 的字符数 (方便提示词里说清楚)
   in_code_block    bool  - prev_content 是否结束在未闭合的代码块内 (奇数个 ```)
   in_table         bool  - prev_content 是否结束在表格行中间 (最后一行以 | 开头但未闭合)

 设计:
   - 用 <PREV>...</PREV> XML 风格标签包 anchor, 避免和 anchor 内的 ``` 冲突;
     LLM 不会把它理解成"代码引用", 而是"原文片段, 必须严格接续".
   - 后端 ResumeStreamDedup 会兜底剥掉前几个字符的重复, 但仍然以提示词
     "不要重复"为主, 减少 LLM 输出的浪费 token.
#}
(系统提示) 你的上一条回复被中断, 请从断点之后**直接**继续完成回答, 一个字也不要重复已输出的内容.

{% if suffix_anchor %}
## 你已输出内容的末尾 ({{ anchor_chars }} 字符, 用 <PREV>...</PREV> 原样包裹)
<PREV>{{ suffix_anchor }}</PREV>

**关键**: 你下一个字符必须紧接 </PREV> 之前最后一个字符之后, 一字一标点一空格都不要重复, 不要先输出换行, 不要寒暄.
{% endif %}

## 续写规则
1. **不重复**: 不要重新输出任何已经写过的文字 / 标点 / 空格 / 换行.
2. **不寒暄**: 不要说 "好的, 我继续" / "继续如下" / "接下来" 等过渡句, 直接写正文.
3. **保持结构对齐**:
   - Markdown 标题 / 列表序号 / 缩进与上文保持一致
   - 不要重复 ``` 代码围栏 / 表格行 / 引用前缀
{% if in_code_block %}
4. **特别注意**: 你正在**代码块内部**写代码 (上文的 ``` 还没闭合).
   - 直接输出代码字符, **不要**在开头加 ``` 或语言标识 (如 ```python)
   - 如果断点在一行中间, 直接补完这一行再换行
   - 等代码完整写完后, 才输出闭合的 ```
   - 保持原有缩进风格 (空格数 / Tab 与上文一致)
{% endif %}
{% if in_table %}
4. **特别注意**: 你正在**Markdown 表格行中间** (上文以 `|` 开头但未闭合).
   - 直接补齐当前行剩余的列, 用 `|` 分隔, 行末以 `|` 闭合
   - 然后再写下一行, 列数与表头保持一致
   - **不要**重复表头行或分隔行 (`|---|---|`)
{% endif %}
