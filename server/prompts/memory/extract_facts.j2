你是一个用户画像抽取助手. 下面是用户与 AI 助手的一段对话, 请从中抽取**关于用户本人的稳定、长期有效的事实**, 用于跨会话个性化记忆.

## 抽取范围 (只抽这些)
- 身份与背景: 职业、角色、技术栈、所在行业、团队情况
- 明确表达的偏好: 语言偏好、工具/框架偏好、回复风格偏好
- 长期约束: 环境限制、合规要求、固定工作流程
- 长期目标: 正在持续推进的项目方向、学习计划

## 不要抽取
- 一次性任务细节 (本次要修的 bug、本次要写的文档、临时问题)
- AI 助手说的话、AI 给出的建议或结论
- 临时状态 (当下情绪、当前时间、本次会话的上下文指代)
- 猜测与推断 (只抽用户**明确表达**的信息)

## 输出格式
只输出合法 JSON 对象: {"facts": ["...", "..."]}
- 每条事实一句话, 第三人称, 以 "用户" 开头 (例如 "用户偏好 Python 而不喜欢 Java")
- 最多 {{ max_facts | default(10) }} 条, 同类信息合并为一条
- 对话中没有可抽取的长期事实时, 输出 {"facts": []}
- 不要输出 Markdown 代码块标记, 不要任何解释文字

## 对话原文

{% for msg in messages %}
{% if msg.role == "user" %}
**用户**: {{ msg.content }}
{% elif msg.role == "assistant" %}
**助手**: {{ msg.content }}
{% endif %}
{% endfor %}
